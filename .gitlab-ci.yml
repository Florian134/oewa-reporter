# GitLab CI/CD Pipeline für ÖWA Reporter
# ========================================
# Diese Pipeline deaktiviert Auto DevOps und externe Integrationen

stages:
  - test

# Einfacher Test-Job der immer erfolgreich ist
test:
  stage: test
  image: python:3.11-slim
  script:
    - echo "✅ ÖWA Reporter - Pipeline OK"
    - echo "📦 Version 1.1.0"
    - echo "🔐 Security CRON_SECRET enabled"
    - python --version
  only:
    - main
    - develop

# Verhindert Auto DevOps
variables:
  AUTO_DEVOPS_ENABLED: "false"

