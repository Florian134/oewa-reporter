# ÖWA Reporter - GitLab CI/CD Pipeline
# =====================================
# Triggered by Airtable Automations or GitLab Schedules

stages:
  - run

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Cache für schnellere Builds
cache:
  paths:
    - .cache/pip/

# =============================================================================
# DAILY INGESTION - Standard Job (wird bei jedem Trigger ausgeführt)
# =============================================================================
daily_ingest:
  stage: run
  image: python:3.11-slim
  script:
    - echo "🚀 ÖWA Reporter - Daily Ingestion"
    - pip install --upgrade pip
    - pip install requests python-dotenv openai
    - python ci_scripts/daily_ingest.py
    - echo "✅ Daily Ingestion completed!"
  rules:
    - if: $JOB_TYPE == "backfill"
      when: never
    - if: $JOB_TYPE == "weekly"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - if: $CI_PIPELINE_SOURCE == "web"

# =============================================================================
# BACKFILL - Historische Daten laden (manuell triggern mit JOB_TYPE=backfill)
# =============================================================================
backfill:
  stage: run
  image: python:3.11-slim
  script:
    - echo "🔄 ÖWA Reporter - Backfill"
    - pip install --upgrade pip
    - pip install requests python-dotenv
    - python ci_scripts/backfill.py --days ${BACKFILL_DAYS:-30}
    - echo "✅ Backfill completed!"
  rules:
    - if: $JOB_TYPE == "backfill"

# =============================================================================
# WEEKLY REPORT - Wöchentlicher Bericht (manuell triggern mit JOB_TYPE=weekly)
# =============================================================================
weekly_report:
  stage: run
  image: python:3.11-slim
  script:
    - echo "📊 ÖWA Reporter - Weekly Report"
    - pip install --upgrade pip
    - pip install requests python-dotenv openai
    - python ci_scripts/weekly_report.py
    - echo "✅ Weekly Report completed!"
  rules:
    - if: $JOB_TYPE == "weekly"

# =============================================================================
# ALERT CHECK - Tägliche Schwellenwert-Prüfung mit GPT-Analyse
# Triggern mit JOB_TYPE=alert_check
# =============================================================================
alert_check:
  stage: run
  image: python:3.11-slim
  script:
    - echo "🚨 ÖWA Reporter - Alert Check"
    - pip install --upgrade pip
    - pip install requests python-dotenv openai
    - python ci_scripts/alert_check.py
    - echo "✅ Alert Check completed!"
  rules:
    - if: $JOB_TYPE == "alert_check"
